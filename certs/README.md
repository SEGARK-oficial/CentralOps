# Certificates

Coloque aqui os arquivos `tls.crt` e `tls.key` caso queira usar um certificado proprio no Nginx.

Se os arquivos nao existirem, o container gera automaticamente um certificado autoassinado em tempo de inicializacao.
